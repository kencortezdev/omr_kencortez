<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Ken's One Month Rails Project", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <!-- Links on the LHS of the page were removed on D10 L1 -->
      <!-- The Search bar here was deleted in D10 L1 -->
      <ul class="nav navbar-nav navbar-right">
        <!-- This is where we put our embedded Ruby links -->
        <!-- Note that the embedded ruby is still nested in the "li" tags" -->
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "About", about_path %></li>
        <!-- Need to call a method delete when a user is logging out -->
        <% if user_signed_in? %>
          <li><%= link_to "Account Settings", edit_user_registration_path %></li>
          <li><%= link_to "Log out", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %></li>
        <% end %>
        <!-- The dropdown ws deleted on D10 L1 -->
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>